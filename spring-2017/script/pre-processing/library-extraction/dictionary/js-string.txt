.length
.charAt(
.charCodeAt(
.concat(
.endsWith(
.fromCharCode(
.includes(
.indexOf(
.lastIndexOf(
.localeCompare(
.match(
.repeat(
.replace(
.search(
.slice(
.split(
.startsWith(
.substr(
.substring(
.toLocaleLowerCase(
.toLocaleUpperCase(
.toLowerCase(
.toUpperCase(
.trim(
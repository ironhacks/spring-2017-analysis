.concat(
.copyWithin(
.every(
.fill(
.filter(
.find(
.findIndex(
.forEach(
.indexOf(
.isArray(
.join(
.lastIndexOf(
.map(
.pop(
.push(
.reduce(
.reduceRight(
.reverse(
.shift(
.slice(
.some(
.sort(
.splice(
.unshift(